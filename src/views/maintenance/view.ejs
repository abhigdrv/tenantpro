<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Request Details</title>
</head>
<body>
    <%- include('../partials/header') %>
    <main>
        <%- include('../partials/sidebar') %>
        <section class="content">
            <h2>Maintenance Request Details</h2>
            <div class="details-card">
                <h3><%= request.title %></h3>
                <p><strong>Property:</strong> <%= request.property.name %></p>
                <p><strong>Tenant:</strong> <%= request.tenant.firstName %> <%= request.tenant.lastName %></p>
                <p><strong>Description:</strong> <%= request.description %></p>
                <p><strong>Priority:</strong> <%= request.priority %></p>
                <p><strong>Status:</strong> <%= request.status %></p>
                <p><strong>Submitted On:</strong> <%= request.createdAt.toLocaleDateString() %></p>
            </div>
            <a href="/maintenance/<%= request.id %>/edit" class="btn">Edit Request</a>
            <a href="/maintenance" class="btn">Back to Requests</a>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>